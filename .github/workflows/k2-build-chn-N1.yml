name: Build K2 OpenWrt 24.10.5 16MB + ZeroTier + WireGuard + Chinese UI

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 360

    steps:
      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y build-essential clang flex bison g++ gawk \
          gcc-multilib g++-multilib gettext git libncurses5-dev libssl-dev \
          python3-setuptools rsync swig unzip zlib1g-dev file wget

      - name: Checkout
        uses: actions/checkout@v4

      - name: Download OpenWrt 24.10.5 source
        run: |
          git clone --depth 1 -b openwrt-24.10 https://github.com/openwrt/openwrt.git
          cd openwrt
          git fetch --tags
          git checkout v24.10.5
          echo "✅ OpenWrt 24.10.5 source downloaded"

      # ========== 1. Show all K2 related DTS files ==========
      - name: Show all K2 related DTS files
        run: |
          cd openwrt
          echo "========================================="
          echo "All K2 related DTS files"
          echo "========================================="
          find target/linux/ramips/dts -name "*k2*" -o -name "*phicomm*" | sort

      # ========== 2. Modify mt7620a_phicomm_k2x.dtsi for 16MB ==========
      - name: Modify mt7620a_phicomm_k2x.dtsi for 16MB
        run: |
          cd openwrt
          DTSI_FILE="target/linux/ramips/dts/mt7620a_phicomm_k2x.dtsi"
          if [ -f "$DTSI_FILE" ]; then
            echo "Original firmware size:"
            grep "reg = <0x50000" "$DTSI_FILE"
            sed -i 's/reg = <0x50000 0xfb0000>/reg = <0x50000 0x1fb0000>/' "$DTSI_FILE"
            echo "Modified firmware size:"
            grep "reg = <0x50000" "$DTSI_FILE"
            echo "✅ 16MB modification successful"
          else
            echo "❌ File not found"
            exit 1
          fi

      # ========== 3. Modify mt7620.mk IMAGE_SIZE ==========
      - name: Modify mt7620.mk IMAGE_SIZE for 16MB
        run: |
          cd openwrt
          MK_FILE="target/linux/ramips/image/mt7620.mk"
          if [ -f "$MK_FILE" ]; then
            echo "Original IMAGE_SIZE:"
            sed -n '/define Device\/phicomm_k2/,/endef/p' "$MK_FILE" | grep "IMAGE_SIZE"
            sed -i '/define Device\/phicomm_k2/,/endef/ s/IMAGE_SIZE := .*/IMAGE_SIZE := 16064k/' "$MK_FILE"
            echo "Modified IMAGE_SIZE:"
            sed -n '/define Device\/phicomm_k2/,/endef/p' "$MK_FILE" | grep "IMAGE_SIZE"
            echo "✅ IMAGE_SIZE modification successful"
          else
            echo "❌ File not found"
            exit 1
          fi

      # ========== 4. Create config file ==========
      - name: Create config file
        run: |
          cd openwrt
          echo "CONFIG_TARGET_ramips=y" > .config
          echo "CONFIG_TARGET_ramips_mt7620=y" >> .config
          echo "CONFIG_TARGET_ramips_mt7620_DEVICE_phicomm_k2=y" >> .config
          echo "CONFIG_TARGET_DEVICE_ramips_mt7620_DEVICE_phicomm_k2=y" >> .config
          echo "CONFIG_TARGET_ROOTFS_PARTSIZE=16" >> .config
          echo "CONFIG_PACKAGE_luci=y" >> .config
          echo "CONFIG_PACKAGE_luci-ssl=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-firewall=y" >> .config
          echo "CONFIG_PACKAGE_luci-i18n-base-zh-cn=y" >> .config
          echo "CONFIG_PACKAGE_luci-i18n-firewall-zh-cn=y" >> .config
          echo "CONFIG_PACKAGE_luci-i18n-opkg-zh-cn=y" >> .config
          echo "CONFIG_LUCI_LANG_zh_Hans=y" >> .config
          echo "CONFIG_PACKAGE_kmod-wireguard=y" >> .config
          echo "CONFIG_PACKAGE_wireguard-tools=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-wireguard=y" >> .config
          echo "CONFIG_PACKAGE_luci-i18n-wireguard-zh-cn=y" >> .config
          echo "CONFIG_PACKAGE_zerotier=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-zerotier=y" >> .config
          echo "CONFIG_PACKAGE_luci-i18n-zerotier-zh-cn=y" >> .config
          echo "CONFIG_PACKAGE_kmod-mt76-core=y" >> .config
          echo "CONFIG_PACKAGE_kmod-mt76x2=y" >> .config
          make defconfig
          echo "✅ Config file created"

      - name: Update feeds
        run: |
          cd openwrt
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Download packages
        run: |
          cd openwrt
          make download -j8

      - name: Compile firmware
        run: |
          cd openwrt
          make tools/compile -j$(nproc)
          make toolchain/compile -j$(nproc)
          make -j$(nproc) V=s

      - name: Organize firmware files
        run: |
          cd openwrt
          mkdir -p firmware
          find bin/targets/ -name "*.bin" -exec cp {} firmware/ \;
          cd firmware
          md5sum *.bin > md5sums.txt
          echo "Firmware files ready"

      # ========== 5. Create simple README ==========
      - name: Create README
        run: |
          cd openwrt/firmware
          echo "Phicomm K2 OpenWrt 24.10.5 Firmware" > README.txt
          echo "==================================" >> README.txt
          echo "" >> README.txt
          echo "Features:" >> README.txt
          echo "- 16MB flash support" >> README.txt
          echo "- WireGuard VPN" >> README.txt
          echo "- ZeroTier" >> README.txt
          echo "- Chinese UI" >> README.txt
          echo "" >> README.txt
          echo "Flashing:" >> README.txt
          echo "- Use Breed" >> README.txt
          echo "- Select Public layout (0x50000)" >> README.txt
          echo "" >> README.txt
          echo "Default IP: 192.168.1.1" >> README.txt
          echo "Username: root" >> README.txt
          echo "Password: (none)" >> README.txt
          echo "" >> README.txt
          echo "Build time: $(date)" >> README.txt
          echo "✅ README created"

      # ========== 6. Upload firmware ==========
      - name: Upload firmware
        uses: actions/upload-artifact@v4
        with:
          name: K2-16M-24.10.5-Firmware
          path: openwrt/firmware/
          retention-days: 30
