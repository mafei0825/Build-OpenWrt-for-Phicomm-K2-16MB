name: Build K2 OpenWrt 24.10.5 Chinese UI

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'OpenWrt version'
        required: true
        default: '24.10.5'
        type: choice
        options:
          - '24.10.5'

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 360

    steps:
      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y build-essential clang flex bison g++ gawk \
          gcc-multilib g++-multilib gettext git libncurses5-dev libssl-dev \
          python3-setuptools rsync swig unzip zlib1g-dev file wget

      - name: Checkout
        uses: actions/checkout@v4

      - name: Download OpenWrt 24.10.5 source
        run: |
          git clone --depth 1 -b openwrt-24.10 https://github.com/openwrt/openwrt.git
          cd openwrt
          git fetch --tags
          git checkout v24.10.5
          echo "✅ OpenWrt 24.10.5 source downloaded"

      - name: Debug - List DTS files
        run: |
          cd openwrt
          echo "Listing all DTS files:"
          find target/linux/ramips/dts -name "*.dts" | grep -i k2 || echo "No K2 DTS files found"
          find target/linux/ramips/dts -name "*.dts" | grep -i phicomm || echo "No Phicomm DTS files found"

      - name: Modify for 16MB flash
        run: |
          cd openwrt
          
          # 方法1: 尝试直接修改DTS文件（如果存在）
          DTS_FILE="target/linux/ramips/dts/mt7620a_phicomm_k2.dts"
          if [ ! -f "$DTS_FILE" ]; then
            DTS_FILE="target/linux/ramips/dts/mt7620n_phicomm_k2.dts"
          fi
          
          if [ -f "$DTS_FILE" ]; then
            echo "Found DTS file: $DTS_FILE"
            cp "$DTS_FILE" "${DTS_FILE}.bak"
            sed -i 's/reg = <0x50000 0xfb0000>/reg = <0x50000 0x1fb0000>/' "$DTS_FILE"
            echo "✅ Modified DTS for 16MB"
            grep -A5 "firmware" "$DTS_FILE" || echo "No firmware partition found"
          else
            echo "⚠️ DTS file not found, will use image builder method"
          fi
          
          # 方法2: 修改Makefile中的IMAGE_SIZE（无论如何都要做）
          MK_FILE="target/linux/ramips/image/mt7620.mk"
          if [ -f "$MK_FILE" ]; then
            echo "Modifying Makefile: $MK_FILE"
            sed -i '/define Device\/phicomm_k2/,/endef/ s/IMAGE_SIZE := .*/IMAGE_SIZE := 16064k/' "$MK_FILE"
            echo "✅ IMAGE_SIZE set to 16MB"
            sed -n '/define Device\/phicomm_k2/,/endef/p' "$MK_FILE" | grep "IMAGE_SIZE"
          else
            echo "❌ Makefile not found"
          fi

      - name: Create config file
        run: |
          cd openwrt
          
          cat > .config << 'EOF'
CONFIG_TARGET_ramips=y
CONFIG_TARGET_ramips_mt7620=y
CONFIG_TARGET_ramips_mt7620_DEVICE_phicomm_k2=y
CONFIG_TARGET_DEVICE_ramips_mt7620_DEVICE_phicomm_k2=y
CONFIG_TARGET_ROOTFS_PARTSIZE=16
CONFIG_PACKAGE_luci=y
CONFIG_PACKAGE_luci-ssl=y
CONFIG_PACKAGE_luci-app-firewall=y
CONFIG_PACKAGE_luci-i18n-base-zh-cn=y
CONFIG_PACKAGE_luci-i18n-firewall-zh-cn=y
CONFIG_LUCI_LANG_zh_Hans=y
CONFIG_PACKAGE_kmod-wireguard=y
CONFIG_PACKAGE_wireguard-tools=y
CONFIG_PACKAGE_luci-app-wireguard=y
CONFIG_PACKAGE_luci-i18n-wireguard-zh-cn=y
CONFIG_PACKAGE_zerotier=y
CONFIG_PACKAGE_luci-app-zerotier=y
CONFIG_PACKAGE_luci-i18n-zerotier-zh-cn=y
CONFIG_PACKAGE_kmod-mt76-core=y
CONFIG_PACKAGE_kmod-mt76x2=y
CONFIG_PACKAGE_iperf3=y
CONFIG_PACKAGE_tcpdump=y
CONFIG_PACKAGE_htop=y
CONFIG_PACKAGE_nano=y
EOF
          
          make defconfig
          echo "✅ .config created"

      - name: Update feeds
        run: |
          cd openwrt
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Download packages
        run: |
          cd openwrt
          make download -j8

      - name: Compile firmware
        run: |
          cd openwrt
          make tools/compile -j$(nproc)
          make toolchain/compile -j$(nproc)
          make -j$(nproc) V=s || make -j1 V=s

      - name: Upload firmware
        uses: actions/upload-artifact@v4
        with:
          name: K2-24.10.5-ChineseUI
          path: openwrt/bin/targets/**/*.bin
          retention-days: 30
